# Honesty table (required by hackathon rules)

| Component | Status | Notes |
|---|---|---|
| spec/PROTOCOL.md | ✅ complete draft | §10 test vectors published under spec/vectors/ (canonical-json, signatures, expiry, state-machine) |
| spec/vectors + sdk vectors test | ✅ working | 19 assertions in test/vectors.test.ts; regenerate with `npm run build && node scripts/gen-vectors.mjs` |
| sdk types/errors | ✅ working | mirrors PROTOCOL exactly |
| sdk canonical JSON + signing digest | ✅ working | RFC 8785 subset; floats rejected by design; unchanged since signature vectors were cut |
| sdk BIP-340 verification + §9 checklist | ✅ working | tamper/expiry/mismatch paths tested; payAnyInvoice now enforces §9 item 3 (quote getAmount == invoice amount) |
| sdk expiry conversions (§6) | ✅ working | single module (src/expiry.ts); asymmetric block→ms constants exercised by expiry vectors |
| sdk invoice module — BOLT11 | ✅ working | payment_hash/amount/expiry via light-bolt11-decoder; tested against SDK-generated invoices (decoder does not verify LN signatures — the hub's nodes are authoritative) |
| sdk invoice module — Fiber | ⚠️ working, format assumption | bech32m HRP amount + fixed payload layout (version, timestamp, payment_hash); round-trip tested against the SDK's own encoder, NOT yet validated against live FNN invoices — production gap |
| sdk client (discover/quotes/orders/watch) | ✅ working, verified against real bifrostd | mock-hub harness covers payAnyInvoice happy path, tampered quote, expired quote, error-envelope passthrough. Also run live 2026-07-15: `Bifrost.payAnyInvoice()` against the real api/ gateway — quote issued, signature-verified client-side, order created, Fiber payment dispatched, order reached SUCCEEDED with matching incoming/outgoing preimages |
| deploy/ dev environment (compose) | ✅ working | verified 2026-07-14: `docker compose up` → fund-regtest.sh → smoke-cch.sh reaches CCH `Success`. Dev-chain topology mirrors upstream fiber e2e CI (decision: local dev chain, not CKB testnet — testnet can't be self-funded reproducibly). Pinned: fiber 0.9.0-rc7, ckb v0.202.0, lnd v0.19.2-beta, bitcoind 29 |
| deploy/scripts/smoke-cch.sh (stock CCH baseline) | ✅ working | full swap: payee BOLT11 → send_btc → wBTC fiber channel → fiber payment → hub pays LN → order `Success` + payee invoice `SETTLED` (upstream has no `Succeeded` state) |
| bifrostd fnn hex codec (`0x…` u64/u128) | ✅ working | single codec module bifrostd/src/fnn/codec.ts, bigint-only, 6 passing tests incl. 0x5f5e100 vector; shell scripts reuse it via codec-cli |
| bifrostd adapters (FiberAdapter + LightningAdapter) | ⚠️ working, fixture-tested | SYSTEM-DESIGN §4.1 bound to real RPC surfaces (FNN new_invoice hold / settle / cancel / send_payment / pubsub; LND invoicesrpc+routerrpc via REST). 31 unit tests on recorded fixtures; contract tests (BIFROST_IT=1) not yet run against live compose env. Divergences logged in docs/RPC-NOTES.md |
| bifrostd rfq (QuoteService + 3 pricing strategies) | ✅ working (unit-tested + live) | Signed quotes via sdk signingDigest + BIP-340; static-peg / feed-spread (stale-feed reject) / inventory-skew composing chain; exact rationals, no floats; §4.2 hub-favorable ≤1-unit rounding proven against the SDK verifier in 16 tests. Now wired to the api/ gateway (staticPeg only — feed-spread/inventory-skew are unit-tested but not selected by index.ts yet) |
| registry/ (Fastify + SQLite discovery service) | ✅ working (e2e-tested) | POST /ads verifies BIP-340 via sdk verifyAdSignature, 5-min/60-s anti-replay window, expiry at issuedAt+ttlMs, verbatim storage (never modifies, never stores/serves rates — rate-bearing payloads rejected); GET /ads pair+amount filters. 7 e2e tests incl tamper/replay/expiry. Single-instance, localhost-bind; no TLS/prod deploy yet |
| dashboard/ (operator UI + swap theater) | ✅ working against real bifrostd | React UI: order table w/ live WS stream, inventory, quote hit-rate, ExpiryGuard health, node connectivity, swap-theater animation. `mock/server.ts` still exists for offline dev (`npm run mock`); dashboard/test/contract.test.ts (9 tests) verified 2026-07-15 against the real gateway via `BIFROSTD_URL=http://127.0.0.1:8391`, all passing. GET /v1/quotes/stats is a PROPOSED §4.5 addition (flagged, not silent) |
| bifrostd orders/ (OrderEngine, R1–R5, I1–I4) | ✅ working (property-tested + live smoke) | State machine per SYSTEM-DESIGN §4.2/PROTOCOL §4.4; each invariant encoded as a seeded property test (15 tests: adversarial event orderings, wrong/missing preimages, concurrent dupes, random crash points + recovery). Persistence is an fsync'd append-only JSONL event log (R5/I4) — SQLite store deferred (production gap: single-file log, no Postgres path yet). ExpiryGuard exists as the engine's I2 checks + sweep, not yet a standalone guard/ module |
| deploy/scripts/smoke-bifrost.sh (engine e2e, both directions) | ✅ working | full swaps through the OrderEngine + real adapters inside the compose stack (runner in the bifrostd container): FIBER_TO_LN then LN_TO_FIBER; verifies preimage equality across legs, payee invoice SETTLED, client fiber invoice Paid |
| bifrostd api/ gateway (bifrostd/src/api/) | ✅ working, v0.1 scope | HTTP+WS server backed by the REAL OrderEngine + adapters (not a simulation): POST /v1/quotes, POST /v1/orders (what sdk's payAnyInvoice actually calls), GET /v1/orders(?state=&cursor=&limit=)/:id, POST /v1/orders/:id/cancel, GET /v1/inventory, GET /v1/health, GET /v1/quotes/stats, WS /v1/stream, plus dev-only GET /v1/demo/invoice and POST /v1/demo/pay (gated off by default via DEMO_ENDPOINTS_ENABLED, see docs/SECURITY.md Finding 3). Verified live 2026-07-16: full end-to-end flow (health → demo invoice → quote → SDK signature verify → order → simulated payment → SUCCEEDED with matching preimages within 2s) — see docs/SECURITY.md's /qa run for the full 10-flow log. I4 crash recovery also verified live (bifrostd restart mid-swap self-healed via engine.start()'s reconciliation). Runs inside the `bifrostd` compose service (docker-compose.dev.yml), port 8391 published 127.0.0.1-only for the dashboard's Vite proxy. **No API-key auth as of 2026-07-17** (docs/SECURITY.md Finding 4 — bearer-token auth was added 2026-07-16, then deliberately removed: the HTLC cryptography is the real security boundary, a quote expires in 30s, and creating an order requires actually paying the Fiber hold invoice, so no payment means no swap and the hub loses nothing; `GET /v1/orders`/`GET /v1/inventory` read exposure is an accepted residual gap for this demo, see SECURITY.md). **Gaps, not silently faked:** no rate limits (docs/SECURITY.md Finding 7); CORS `Access-Control-Allow-Origin: *` (Finding 6); no webhooks (events/ §4.6 not started); quote issuance is in-memory (QuoteCache) — lost on restart, a short seconds-long window; only QuoteMode PAY_INVOICE is implemented (RECEIVE returns a clear error); no external price feed wired (staticPeg only, health always reports feed.fresh=true) |
| bifrostd hub config / HUB_MODE (bifrostd/src/config.ts) | ⚠️ working, partial wiring | `resolveHubConfig()` supports `HUB_MODE=dev\|managed\|external`: dev auto-generates an ephemeral signing key (warns loudly) and defaults to the compose stack's fixed ports; managed/external require `HUB_SIGNING_KEY` (generate via `node scripts/generate-hub-key.mjs`), and external additionally requires `LND_TLS_CERT_PATH`/`LND_MACAROON_PATH` (macaroon read once at startup, sent as a header — no wallet seed ever touched) and refuses to start with `DEMO_ENDPOINTS_ENABLED=true`. Verified live 2026-07-18: full dev-mode regression (`deploy/scripts/fund-regtest.sh` + `smoke-bifrost.sh`, both swap directions SUCCEEDED) against the new config path, plus `bifrostd`'s own test suite (7 new config tests, 81/87 passing overall, 6 pre-existing integration tests skipped as before). **Gaps, not silently faked:** `LND_WALLET_PASSWORD` is accepted/documented but not wired to any unlock RPC; `FNN_CONFIG_DIR`/`FNN_PRIVATE_KEY` from the original ask are not implemented — bifrostd only ever calls an already-running FNN node's RPC and never reads its config dir or key, so those vars don't apply to bifrostd itself; `HUB_FEE_PPM`/`QUOTE_STRATEGY` are documented in `deploy/.env.example` but not yet wired into `index.ts` (pricing is still hardcoded 1:1 no-fee staticPeg, a pre-existing gap, not new). See `docs/EXTERNAL-NODE-SETUP.md` for the external-mode operator walkthrough |
| events/ webhooks | ❌ not started | §4.6 EventBus/WebhookDispatcher; the WS /v1/stream in api/ is a direct StreamHub broadcast, not built on a general event bus yet |
| demo/ (merchant checkout, Vite + React) | ✅ working against real bifrostd | Merchant dashboard (`/`) generates checkout links + test invoices; customer checkout (`/checkout`) gets a signed quote (client-verified via `verifyQuote`), creates an order, shows the Fiber hold invoice as a QR, has a demo-only "Simulate Payment" button, polls to SUCCEEDED, shows preimage/payment-hash/order-id as proof of payment. `npm run build` clean. Sends no auth header (bifrostd has none as of 2026-07-17, see docs/SECURITY.md Finding 4). No CKB/Lightning block-explorer links — Fiber and Lightning both settle off-chain per-swap (no per-payment on-chain tx exists on either leg); verified by preimage instead |
