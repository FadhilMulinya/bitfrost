{
  "name": "@bifrost/registry",
  "version": "0.0.1",
  "description": "bifrost-registry: untrusted discovery service for signed hub advertisements (PROTOCOL §4.5). Metadata only — never stores or serves rates.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "license": "MIT",
  "dependencies": {
    "bifrost-sdk": "file:../sdk",
    "better-sqlite3": "^11.3.0",
    "fastify": "^4.28.0"
  },
  "devDependencies": {
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0",
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": { "node": ">=20" }
}
