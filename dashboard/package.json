{
  "name": "@bifrost/dashboard",
  "version": "0.0.1",
  "description": "Operator UI (SYSTEM-DESIGN §4.10) against MOCKED bifrostd §4.5 endpoints. mock/ doubles as the API contract test for bifrostd.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "mock": "node --experimental-strip-types mock/server.ts",
    "build": "tsc -p tsconfig.json && vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "dependencies": {
    "@bifrost/sdk": "file:../sdk",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@types/ws": "^8.5.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0",
    "ws": "^8.18.0"
  },
  "engines": { "node": ">=20" }
}
